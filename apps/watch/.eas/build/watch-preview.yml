build:
  name: Build Watch APK
  steps:
    - eas/checkout
    - eas/inject_android_credentials
    - run:
        name: Build Watch APK
        command: cd apps/watch && ./gradlew :app:assembleRelease
    - eas/find_and_upload_build_artifacts
