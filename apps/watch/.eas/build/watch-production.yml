build:
  name: Build Watch AAB
  steps:
    - eas/checkout
    - eas/inject_android_credentials
    - run:
        name: Build Watch AAB
        command: cd apps/watch && ./gradlew :app:bundleRelease
    - eas/find_and_upload_build_artifacts
